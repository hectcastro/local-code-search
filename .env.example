# Copy this file to .env and customize for your local setup
# cp .env.example .env

# Host directory containing git repositories to index
# Example: /Users/yourname/Projects or /home/yourname/code
HOST_REPOS=/Users/yourname/Projects

# Host directory for storing Zoekt index files (optional)
# Default: ~/zoekt/index
# HOST_INDEX=/Users/yourname/zoekt/index

# Port for accessing the web UI (optional)
# Default: 6070
# HOSTPORT=6070

# Kubernetes cluster name (optional)
# Default: zoekt
# K3D_CLUSTER=zoekt

# Kubernetes namespace (optional)
# Default: zoekt
# NS=zoekt

# GitLab repository cloning configuration
# Configure these variables to use 'make clone-repos'

# Base URL for GitLab instance (optional)
# Default: https://gitlab.com
# GITLAB_URL=https://gitlab.com

# Personal access token with 'read_api' scope (required for clone-repos)
# Create at: GitLab → Preferences → Access Tokens
# GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx

# Clone type: 'group' or 'user' (optional)
# Default: group
# GITLAB_CLONE_TYPE=group
# GITLAB_CLONE_TYPE=user

# Target to clone: group name, username, or 'all-users' (required for clone-repos)
# For groups: GITLAB_GROUP=mygroup
# For user repos: GITLAB_GROUP=username
# For all users: GITLAB_GROUP=all-users (requires GitLab 13.0.1+)
# GITLAB_GROUP=mygroup

# Additional ghorg options (optional)
# Default: --preserve-dir
# GHORG_CLONE_OPTS=--preserve-dir
